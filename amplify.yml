version: 1

frontend:
  phases:
    preBuild:
      commands:
        - nvm use 22.14.0
        - corepack enable
        - pnpm install --frozen-lockfile

    build:
      commands:
        - env | grep -e EMAIL_ADDRESS >> .env.production
        - pnpm build

  artifacts:
    baseDirectory: .next
    files:
      - '**/*'

  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
